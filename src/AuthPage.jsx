/**
 * ⚠️  DEPRECATED — DO NOT USE
 *
 * This file is dead code. It was never imported or rendered anywhere in the app.
 * The canonical auth UI lives inside `src/App.jsx` as the `HomePage` component,
 * which handles Sign In, Create Account, and Forgot Password all in one place
 * with full Firebase Auth integration.
 *
 * This file is kept temporarily so the git diff is visible. It should be
 * deleted in the next cleanup commit:
 *
 *   git rm src/AuthPage.jsx
 *
 * If you ever want a dedicated standalone auth page, start from `HomePage`
 * in App.jsx rather than from this file.
 */

// (no exports — intentionally empty)
